apiVersion: v1
kind: Service
metadata:
  name: {{ include "haylen.fullname" . }}
  labels:
    {{- include "haylen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "haylen.selectorLabels" . | nindent 4 }}
