apiVersion: v1
kind: Service
metadata:
  name: {{ include "weston.fullname" . }}
  labels:
    {{- include "weston.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "weston.selectorLabels" . | nindent 4 }}
